/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['edamam-product-images.s3.amazonaws.com'], // Allow this domain for images
    },
  };
  
  export default nextConfig;
  